#include<iostream>
double sequence(double x, double n);

int main ()
{


}
double sequence(double x, double n)
{
  if (x%2==0 && x!=1)
    {
      return sequence(x/2,n+1);
    }
  else
    {
      if(x!=1)
	{
	  return sequence(3*x+1,n+1);
	}
    }
}
